# Glance Weather App

## Frontend

- **Component library: Chakra UI** — prefer Chakra components (`Text`, `Box`, `Flex`, etc.) over raw HTML elements (`div`, `span`, `p`, etc.) whenever a suitable Chakra component exists
